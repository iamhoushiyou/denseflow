 项目依赖:
 
  1.cuda8.0 + cudnn 6.0 
  2.opencv 3.1
  3.libzip
     sudo apt-get install libzip-dev
  4.一系列编译工具
 编译方法：
 １．cd ${your directory}/denseflow
 2. sudo mkdir build & cd nuild
 3. sudo cmake ..
 4. sudo make
 
 
